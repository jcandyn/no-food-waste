<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Zero Waste</title>
    <link rel="stylesheet" href="/public/css/main.css">
    <link rel="shortcut icon" type="image/x-icon" href="../../public/assets/planet.png" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a id="logo-link" class="navbar-brand" href="/">
                <img src="../../public/assets/planet.png" alt="Logo" width="50" height="50">
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    {{#unless name}}
                    <li class="nav-item active">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    {{/unless}}
                    <li class="nav-item">
                        <a class="nav-link" href="/about">About</a>
                    </li>
                    {{#if name}}
                    <li class="nav-item">
                        <a class="nav-link" href="/food">Inventory</a>
                    </li>
                    {{/if}}
                    {{#if name}}
                    <li class="nav-item">
                        <a class="nav-link" href="/sharing">Sharing</a>
                    </li>
                    {{/if}}
                     {{#if name}}
                    <li class="nav-item">
                        <a class="nav-link" href="/analytics">Analytics</a>
                    </li>
                    {{/if}}
                    {{#if name}}
                    <li class="nav-item">
                        <a class="nav-link" href="/composting">Composting</a>
                    </li>
                    {{/if}}
                    {{#if name}}
                    <li class="nav-item">
                        <a class="nav-link" href="/storage">Storage</a>
                    </li>
                    {{/if}}
                </ul>

                <!-- Profile Section should only show if there is an user in session -->
                <div class="navbar-nav">
                     {{#if name}}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Profile
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <!-- Display the name of the logged-in user -->
                            <li><span class="dropdown-item">Welcome, {{name}}!</span></li>
                            <!-- Add a logout option -->
                            <li><a class="dropdown-item" href="/authenticate/logout">Logout</a></li>
                        </ul>
                    </li>
                    {{/if}}
                </div>
            </div>
        </nav>
    </header>
    <main>
        {{{body}}}
    </main>
    <!-- <footer>I'm a footer</footer> -->
</body>

</html>